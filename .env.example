# Copy to .env (which is git-ignored) and fill in. Never commit real keys.
# Jev / TypeSafe AI (https://console.typesafe.ai). Either name works.
Jev_api_key=
# TYPESAFE_API_KEY=

# Optional: hosted open-weights vision model (used only as a reading aid for scans)
# VISION_API_KEY=
