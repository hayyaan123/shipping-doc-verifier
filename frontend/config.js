// Where the backend API lives. "" means "same server as this page" (local use, or the backend serving the frontend).
// For a separately hosted frontend, set the backend's address, e.g. window.SDV_API = "https://shipping-doc-verifier.onrender.com";
// It can also be given once in the address bar: https://<frontend>/?api=https://<backend>
window.SDV_API = "";
